'use client'

import { useMemo, useState } from 'react'
import type { ReactNode } from 'react'
import { ArrowLeft, BadgeCheck, BriefcaseBusiness, CalendarDays, CheckCircle2, CircleHelp, Languages, LockKeyhole, LogOut, MapPin, MessageCircle, Search, ShieldCheck, Sparkles, Star, TrendingUp, Users, Vote, WalletCards, X, Home, Bell } from 'lucide-react'
import { gigs, students } from '@/lib/pipull-data'
import { rankMatches, getRoleLabel, type DemoAccount } from '@/lib/pipull-demo'
import type { Gig, Student } from '@/lib/pipull-data'
import { Avatar } from './avatar'
import { seededTradeCounts } from '@/lib/pipull-trades'

type ViewMode = 'customer' | 'worker'

export function RoleDashboard({ account, onLogout, onBook, onHire }: { account: DemoAccount; onLogout: () => void; onBook: (gig: Gig) => void; onHire: (student: Student) => void }) {
  const initialMode: ViewMode = account.role === 'recruiter' ? 'customer' : 'worker'
  const [mode, setMode] = useState<ViewMode>(initialMode)
  const [query, setQuery] = useState('')
  const [language, setLanguage] = useState('English')
  const [chatWith, setChatWith] = useState<string | null>(null)
  const [notice, setNotice] = useState('')
  const rankedGigs = useMemo(() => rankMatches(query, gigs), [query])
  const rankedStudents = useMemo(() => rankMatches(query, students), [query])
  const isCustomer = mode === 'customer'

  function showNotice(text: string) { setNotice(text); window.setTimeout(() => setNotice(''), 2800) }

  return <main className="min-h-dvh bg-surface text-foreground">
    <header className="border-b border-border bg-card"><div className="mx-auto flex max-w-7xl items-center justify-between gap-4 px-4 py-4 sm:px-6 lg:px-8"><button type="button" onClick={() => window.location.assign('/')} className="flex items-center gap-3 text-left" aria-label="Return to PiPull home"><div className="flex size-10 items-center justify-center rounded-xl bg-brand text-sm font-black text-brand-foreground">P</div><div><p className="font-mono text-sm font-bold tracking-[0.18em] text-brand">PIPULL</p><p className="text-xs text-muted-foreground">Pipull pulls people · {getRoleLabel(account.role)}</p></div></button><div className="relative flex items-center gap-2"><button type="button" onClick={() => showNotice('You are all caught up.')} aria-label="Notifications" className="rounded-lg border border-border p-2 text-muted-foreground hover:bg-accent hover:text-brand"><Bell className="size-4" /></button><details className="relative"><summary className="flex cursor-pointer list-none items-center gap-2 rounded-xl border border-border px-2 py-1.5 text-sm font-semibold hover:bg-accent"><Avatar name={account.name} color="#2563eb" className="size-8 text-xs" /><span className="hidden sm:block">{account.name}</span><ChevronDown className="size-4 text-muted-foreground" /></summary><div className="absolute right-0 top-12 z-30 w-52 rounded-xl border border-border bg-card p-2 shadow-xl"><p className="px-3 py-2 text-xs text-muted-foreground">Switch workspace</p><button type="button" onClick={() => setMode('customer')} className={`flex w-full items-center gap-2 rounded-lg px-3 py-2 text-left text-sm font-semibold ${isCustomer ? 'bg-accent text-brand' : 'hover:bg-surface'}`}><Home className="size-4" /> Customer view</button><button type="button" onClick={() => setMode('worker')} className={`flex w-full items-center gap-2 rounded-lg px-3 py-2 text-left text-sm font-semibold ${!isCustomer ? 'bg-accent text-brand' : 'hover:bg-surface'}`}><BriefcaseBusiness className="size-4" /> Worker view</button><button type="button" onClick={onLogout} className="mt-1 flex w-full items-center gap-2 rounded-lg px-3 py-2 text-left text-sm font-semibold text-red-600 hover:bg-red-50"><LogOut className="size-4" /> Sign out</button></div></details></div></div></header>
    <div className="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8">
      <div className="flex flex-col justify-between gap-5 lg:flex-row lg:items-end"><div><p className="flex items-center gap-2 text-sm font-semibold text-brand"><Sparkles className="size-4" /> {isCustomer ? 'Fair matching for your neighborhood' : 'Your cooperative workspace'}</p><h1 className="mt-2 max-w-3xl text-balance text-3xl font-bold tracking-tight sm:text-5xl">{isCustomer ? 'Find trusted local talent.' : 'Build a livelihood that belongs to you.'}</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-muted-foreground">{isCustomer ? 'Describe the work in plain language. Pipull ranks verified people without surge pricing or top-worker bias.' : 'Track jobs, earnings, training, and your voice in the worker-owned marketplace.'}</p></div><div className="flex items-center gap-2 rounded-xl border border-verified/20 bg-verified/10 px-3 py-2 text-xs font-bold text-verified"><ShieldCheck className="size-4" /> Fair Allocation Mode Active</div></div>
      {isCustomer ? <>
        <label className="mt-8 flex min-h-14 items-center gap-3 rounded-2xl border border-input bg-card px-4 shadow-sm focus-within:border-brand focus-within:ring-4 focus-within:ring-brand/10"><Search className="size-5 text-muted-foreground" /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search for a service, skill, or location" className="w-full bg-transparent text-sm outline-none placeholder:text-muted-foreground" /><span className="hidden items-center gap-1 rounded-lg bg-accent px-2 py-1 text-xs font-bold text-brand sm:flex"><CircleHelp className="size-3" /> Match &gt;40%</span></label>
        {query.trim() ? <>
          <section className="mt-6 flex flex-wrap items-center gap-3 text-xs"><span className="inline-flex items-center gap-1 rounded-full border border-border bg-card px-3 py-2 font-semibold"><BadgeCheck className="size-4 text-verified" /> KYC verified</span><span className="inline-flex items-center gap-1 rounded-full border border-border bg-card px-3 py-2 font-semibold"><LockKeyhole className="size-4 text-brand" /> Secure contact</span><span className="inline-flex items-center gap-1 rounded-full border border-border bg-card px-3 py-2 font-semibold"><Languages className="size-4 text-brand" /> {language}</span></section>
          <div className="mt-8 flex items-center justify-between"><h2 className="text-lg font-bold">Top semantic matches</h2><span className="text-xs text-muted-foreground">Sorted by relevance</span></div>
          <div className="mt-4 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{rankedStudents.map(({ item, score }) => <article key={item.id} className="rounded-2xl border border-border bg-card p-5 shadow-sm transition hover:-translate-y-0.5 hover:border-brand"><div className="flex items-start justify-between gap-3"><div className="flex items-center gap-3"><Avatar name={item.name} color={item.avatarColor} className="size-11 text-sm" /><div><h3 className="font-bold">{item.name}</h3><p className="text-xs text-muted-foreground">{item.primarySkill} · <MapPin className="inline size-3" /> Delhi/NCR</p></div></div><span className="rounded-full bg-verified/10 px-2 py-1 text-xs font-bold text-verified">{score}% match</span></div><div className="mt-4 flex items-center gap-3 text-xs text-muted-foreground"><span className="inline-flex items-center gap-1 text-amber-600"><Star className="size-3.5 fill-current" /> {item.rating}</span><span>{item.completedGigs} jobs</span><span className="text-verified">Available today</span></div><div className="mt-4 flex flex-wrap gap-1.5"><span className="rounded-md bg-accent px-2 py-1 text-[11px] font-semibold text-brand">Skill certified</span><span className="rounded-md bg-surface px-2 py-1 text-[11px] font-semibold text-muted-foreground">Verified profile</span></div><div className="mt-5 flex gap-2"><button type="button" onClick={() => onHire(item)} className="min-h-10 flex-1 rounded-lg bg-brand px-3 text-xs font-bold text-brand-foreground hover:opacity-90">Hire / Request Gig</button><button type="button" onClick={() => setChatWith(item.name)} aria-label={`Message ${item.name}`} className="rounded-lg border border-border px-3 text-brand hover:bg-accent"><MessageCircle className="size-4" /></button></div><button type="button" onClick={() => showNotice(`${item.name}'s availability: today, 9 AM–6 PM`)} className="mt-3 flex items-center gap-1 text-xs font-semibold text-muted-foreground hover:text-brand"><CalendarDays className="size-3.5" /> View schedule & availability</button></article>)}</div>
        </> : <div className="mt-8 rounded-2xl border border-dashed border-border bg-card p-8 text-center"><Search className="mx-auto size-8 text-muted-foreground" /><h2 className="mt-3 font-bold">Search when you need local talent</h2><p className="mt-1 text-sm text-muted-foreground">Suggestions, filters, locations, and matches will appear after you enter a search.</p></div>}
      </> : <WorkerWorkspace account={account} onHire={onHire} showNotice={showNotice} />}
      <section className="mt-10 rounded-2xl border border-border bg-card p-5"><div className="flex items-center justify-between gap-4"><div><h2 className="font-bold">25-trade marketplace seed</h2><p className="mt-1 text-sm text-muted-foreground">Core services start with active profiles and local availability.</p></div><span className="rounded-full bg-verified/10 px-2.5 py-1 text-xs font-bold text-verified">Live seed</span></div><div className="mt-4 flex flex-wrap gap-2">{seededTradeCounts.map((trade) => <span key={trade.name} className="rounded-full border border-border bg-surface px-3 py-1.5 text-xs font-medium text-muted-foreground">{trade.name}</span>)}</div></section>
      <button type="button" onClick={onLogout} className="mt-8 inline-flex items-center gap-2 text-sm font-semibold text-muted-foreground hover:text-foreground"><ArrowLeft className="size-4" /> Sign out and return to login</button>
    </div>
    {notice && <div role="status" className="fixed bottom-5 left-1/2 z-50 flex -translate-x-1/2 items-center gap-2 rounded-xl bg-foreground px-4 py-3 text-sm font-semibold text-background shadow-xl"><CheckCircle2 className="size-4 text-verified" /> {notice}</div>}
    {chatWith && <div className="fixed inset-0 z-50 flex items-end justify-center bg-foreground/40 p-4 sm:items-center"><section role="dialog" aria-modal="true" aria-label="Secure message" className="w-full max-w-md rounded-2xl border border-border bg-card p-5 shadow-xl"><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-wider text-brand">Secure conversation</p><h2 className="mt-1 text-xl font-bold">Message {chatWith}</h2></div><button type="button" onClick={() => setChatWith(null)} aria-label="Close message"><X className="size-5 text-muted-foreground" /></button></div><div className="mt-5 rounded-xl bg-surface p-4 text-sm text-muted-foreground">Phone numbers are hidden — India DPDP Act compliant. Chat or start an in-app VoIP call without sharing personal details.</div><button type="button" onClick={() => { setChatWith(null); showNotice('Message composer opened securely.') }} className="mt-4 min-h-11 w-full rounded-lg bg-brand font-bold text-brand-foreground">Start secure chat</button></section></div>}
  </main>
}

function WorkerWorkspace({ account, onHire, showNotice }: { account: DemoAccount; onHire: (student: Student) => void; showNotice: (text: string) => void }) {
  return <section className="mt-8 grid gap-4 lg:grid-cols-[1.3fr_0.7fr]"><div className="grid gap-4 sm:grid-cols-2"><article className="rounded-2xl bg-brand p-5 text-brand-foreground sm:col-span-2"><div className="flex items-start justify-between"><div><p className="text-sm text-brand-foreground/75">This week&apos;s earnings</p><p className="mt-2 text-4xl font-black">₹8,450</p><p className="mt-2 text-xs text-brand-foreground/75">+18% from last week · 6 completed jobs</p></div><WalletCards className="size-7" /></div><div className="mt-5 flex items-center justify-between border-t border-brand-foreground/20 pt-3 text-xs"><span>Quarterly surplus redistribution</span><span className="font-bold">₹1,240 pending vote</span></div></article><Metric icon={<BriefcaseBusiness />} title="Active requests" value="3" detail="2 nearby · 1 scheduled" /><Metric icon={<TrendingUp />} title="Reputation" value="4.9 / 5" detail="Top 12% in Delhi/NCR" /><article className="rounded-2xl border border-border bg-card p-5 sm:col-span-2"><div className="flex items-center justify-between"><div><p className="text-sm text-muted-foreground">Cooperative Mutual Fund</p><p className="mt-1 text-2xl font-black">₹24,680</p></div><Vote className="size-6 text-brand" /></div><p className="mt-2 text-xs leading-5 text-muted-foreground">Peer-verified relief funds are available for emergencies and family support.</p><button type="button" onClick={() => showNotice('Emergency claim draft started for peer verification.')} className="mt-4 inline-flex items-center gap-2 rounded-lg border border-brand px-3 py-2 text-xs font-bold text-brand hover:bg-accent"><Vote className="size-4" /> Submit / Vote on emergency claim</button></article></div><aside className="flex flex-col gap-4"><article className="rounded-2xl border border-border bg-card p-5"><div className="flex items-center gap-2"><Sparkles className="size-5 text-brand" /><h2 className="font-bold">Weekly incentives</h2></div><p className="mt-4 text-2xl font-black">₹750 bonus</p><p className="mt-1 text-xs text-muted-foreground">Complete 2 more neighborhood jobs this week.</p><div className="mt-4 h-2 rounded-full bg-surface"><div className="h-2 w-2/3 rounded-full bg-brand" /></div></article><article className="rounded-2xl border border-border bg-card p-5"><h2 className="font-bold">Learn & connect</h2><div className="mt-4 flex flex-col gap-3 text-sm"><button type="button" onClick={() => showNotice('Onboarding courses are opening soon.')} className="flex items-center gap-3 text-left font-semibold hover:text-brand"><CheckCircle2 className="size-4 text-verified" /> Onboarding courses</button><button type="button" onClick={() => showNotice('Team Up request sent to nearby workers.')} className="flex items-center gap-3 text-left font-semibold hover:text-brand"><Users className="size-4 text-brand" /> Team Up for group hiring</button><button type="button" onClick={() => showNotice('Community forum opened.')} className="flex items-center gap-3 text-left font-semibold hover:text-brand"><MessageCircle className="size-4 text-brand" /> Internal worker forum</button></div></article><CirclesCard showNotice={showNotice} /></aside></section>
}

function CirclesCard({ showNotice }: { showNotice: (text: string) => void }) {
  return <article className="rounded-2xl border border-brand/20 bg-accent/40 p-5"><div className="flex items-center gap-2"><Users className="size-5 text-brand" /><h2 className="font-bold">Skill Circles</h2></div><p className="mt-2 text-xs leading-5 text-muted-foreground">Team up with nearby members for larger jobs and share credit fairly.</p><div className="mt-4 flex flex-wrap gap-2"><span className="rounded-full bg-card px-2.5 py-1 text-xs font-semibold">Home refresh</span><span className="rounded-full bg-card px-2.5 py-1 text-xs font-semibold">Event crew</span><span className="rounded-full bg-card px-2.5 py-1 text-xs font-semibold">Study sprint</span></div><button type="button" onClick={() => showNotice('Skill Circle invite sent to nearby members.')} className="mt-4 w-full rounded-lg border border-brand px-3 py-2 text-xs font-bold text-brand hover:bg-card">Create a Circle</button></article>
}

function Metric({ icon, title, value, detail }: { icon: ReactNode; title: string; value: string; detail: string }) { return <article className="rounded-2xl border border-border bg-card p-5"><div className="flex items-center gap-2 text-brand">{icon}<span className="text-sm font-semibold text-muted-foreground">{title}</span></div><p className="mt-4 text-2xl font-black">{value}</p><p className="mt-1 text-xs text-muted-foreground">{detail}</p></article> }

